{#
/**
* @file
* Theme override to display a block.
*
* Available variables:
* - plugin_id: The ID of the block implementation.
* - label: The configured label of the block if visible.
* - configuration: A list of the block's configuration values.
*   - label: The configured label for the block.
*   - label_display: The display settings for the label.
*   - provider: The module or other provider that provided this block plugin.
*   - Block plugin specific settings will also be stored here.
* - content: The content of this block.
* - attributes: array of HTML attributes populated by modules, intended to
*   be added to the main container tag of this template.
*   - id: A valid HTML ID and guaranteed unique.
* - title_attributes: Same as attributes, except applied to the main title
*   tag that appears in the template.
* - title_prefix: Additional output populated by modules, intended to be
*   displayed in front of the main title tag that appears in the template.
* - title_suffix: Additional output populated by modules, intended to be
*   displayed after the main title tag that appears in the template.
*
* @see template_preprocess_block()
*/
#}
{# Attach the library #}
{{ attach_library('cua_theme/half-and-half') }}
{# Set CSS Classes #}
{% set container_classes = [
    'block',
    'block-' ~ configuration.provider|clean_class,
    'block-' ~ plugin_id|clean_class,
] %}
{# Preprocess #}
{% set color_background = (content.field_color_background.0) %}
{% if color_background is not empty %}
    {% set background_class = color_background %}
{% endif %}
{% set border_style = (content.field_border_style.0 ) %}
{% set image_alt = (content.field_image.0['#item'].alt ) %}
{% set image_size = (content.field_image_size.0 ) %}
{% set image_align = (content.field_image_aligment.0 ) %}
{% set image_path = (content.field_image['#items'].entity.uri.value) ? (file_url(content.field_image['#items'].entity.uri.value)) : '' %}
{% set link_title = (content.field_link.0['#title']|render) %}
{% set link_url = file_url(content.field_link.0['#url']|render) %}
{% set padding = (content.field_padding.0 ) %}
{% set text_align = (content.field_text_alignment.0 ) %}
{% set outline = (content.field_outline.0 ) %}
{# content #}
<div{{ attributes.addClass('half-and-half g-0 p-0 m-0', background_class ) }}>
    <div class="container d-block d-sm-none g-0">
        <div class="card {{ border_style }}">
            <div class="row g-0">
                <div class="col-12 {{ background_class }} {{ text_align }}">
                    {% include '@cua_theme/templates/block/half_and_half/hh--images.html.twig' %}
                </div>
                <div class="col-12 {{ background_class }} {{ text_align }}">
                    {% include '@cua_theme/templates/block/half_and_half/hh--card-body.html.twig' %}
                </div>
            </div>
        </div>
    </div>
    <div class="container d-none d-sm-block g-0">
        <div class="card {{ border_style }}">
            <div class="row ">
                {% if (content.field_custom_layout[0] is not empty) and (content.field_custom_layout.value != "4") %}
                <div class="col-md-4 col-sm-12 {{ background_class }} {{ text_align }} {{ image_align }}">
                    {% else %}
                    <div class="col-md-6 col-sm-12 {{ background_class }} {{ text_align }} {{ image_align }}">
                        {% endif %}
                        {% include '@cua_theme/templates/block/half_and_half/hh--images.html.twig' %}
                    </div>
                    {% if (content.field_custom_layout[0] is not empty) and (content.field_custom_layout.value != "4") %}
                    <div class="col-md-8 col-sm-12 {{ background_class }} {{ text_align }}">
                        {% else %}
                        <div class="col-md-6 col-sm-12 {{ background_class }} {{ text_align }}">
                            {% endif %}
                            {% include '@cua_theme/templates/block/half_and_half/hh--card-body.html.twig' %}
                        </div>
                    </div>
                </div>
            </div>
        </div>