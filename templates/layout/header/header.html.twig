{#
/**
 * @file: header.html.twig
 * Standards: WCAG 2.1 AA / Section 508 Compliant.
 * * Key Constraints:
 * - Branding: site_name/slogan use |e (Drupal translates upstream).
 * - Accessibility: Search h2 (#search-heading) must remain outside the collapse div.
 * - Navigation: Mobile toggle sits in top bar but controls #navbar-main via ID.
 * - Performance: Secondary menu rendered once; repositioned via CSS/Bootstrap.
 */
#}

<header id="header" class="header" role="banner" aria-label="{{ 'Site header'|t }}">

	<div class="container top-bar mb-2">
		<div class="row align-items-center clearfix g-0">
			<div class="col mt-3 g-0">
				<div class="navbar l-header-top navbar-light bg-white p-0 m-0" id="navbar-top">
					<div class="container-fluid site-name-logo g-0">
						<div class="container d-flex g-0">

							<div class="flex-shrink-1 d-flex align-items-center">
								<a href="{{ url('<front>') }}" rel="home" class="site-branding__logo d-flex align-items-center text-decoration-none">
									<img class="d-none d-lg-block me-2" src="{{ base_path ~ directory ~ '/images/logo.svg' }}" alt="{{ 'University of Colorado'|t }}" width="225" height="24" fetchpriority="high" loading="eager"/>
									<img class="d-lg-none me-2" src="{{ base_path ~ directory ~ '/images/cu-mobile-logo.svg' }}" alt="{{ 'University of Colorado'|t }}" width="29" height="28"/>
									<span class="site-slogan text-nowrap">
										{{ site_slogan|e }}
									</span>
								</a>
							</div>

							{% if user.hasPermission('administer site configuration') %}
								<div class="ms-auto d-none d-lg-flex align-items-center">
									<a href="/tutorials" class="btn btn-sm btn-admin-app">
										{{ 'Tutorials'|t }}
									</a>
								</div>
							{% endif %}

							<div class="d-lg-none ms-auto">
								<button class="navbar-toggler collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#mainNavbarSupportedContent" aria-controls="mainNavbarSupportedContent" aria-expanded="false" aria-label="{{ 'Toggle navigation'|t }}">
									<span class="navbar-toggler-icon"></span>
								</button>
							</div>

						</div>
					</div>
				</div>
			</div>
		</div>
	</div>

	{% if page.search %}
		<div class="container-fluid main-search bg-light border-top border-grey">
			<div class="container">
				<h2 id="search-heading" class="visually-hidden">
					{{ 'Search site'|t }}
				</h2>
				<div class="collapse" id="CollapseSearch" role="region" aria-labelledby="search-heading">
					<div class="cua-search">
						{{ page.search }}
					</div>
				</div>
			</div>
		</div>
	{% endif %}

	<div class="container-fluid bg-light border-bottom border-grey px-3 py-2">
		<div class="container d-flex justify-content-between align-items-center">
			{% if site_name %}
				<div class="site-slogan mx-2 my-2">
					{{ site_name|e }}
				</div>
			{% endif %}

			{% if page.search %}
				<div class="search-button">
					<button class="search-icon btn btn-link p-0 text-dark" type="button" data-bs-toggle="collapse" data-bs-target="#CollapseSearch" aria-expanded="false" aria-controls="CollapseSearch" aria-label="{{ 'Open search'|t }}">
						<svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-search" viewbox="0 0 16 16" aria-hidden="true" focusable="false">
							<path d="M11.742 10.344a6.5 6.5 0 1 0-1.397 1.398h-.001q.044.06.098.115l3.85 3.85a1 1 0 0 0 1.415-1.414l-3.85-3.85a1 1 0 0 0-.115-.1zM12 6.5a5.5 5.5 0 1 1-11 0 5.5 5.5 0 0 1 11 0"/>
						</svg>
					</button>
				</div>
			{% endif %}
		</div>
	</div>

	{% if page.primary_menu or page.secondary_menu %}
		<nav class="navbar l-header-bottom navbar-expand-lg shadow-sm g-0 m-0" id="navbar-main" aria-label="{{ 'Main menu'|t }}">
			<div class="container px-3 g-0">
				<div class="collapse navbar-collapse" id="mainNavbarSupportedContent">
					<img class="sticky-show" src="{{ base_path ~ directory ~ '/images/cu-mobile-logo.svg' }}" alt="" width="25" height="24" aria-hidden="true"/>

					{% if page.primary_menu %}
						<div class="primary-menu-wrapper">
							{{ page.primary_menu }}
						</div>
					{% endif %}

					{% if page.secondary_menu %}
						<div class="secondary-menu-container ms-lg-auto">
							{{ page.secondary_menu }}
						</div>
					{% endif %}
				</div>
			</div>
		</nav>
	{% endif %}

</header>