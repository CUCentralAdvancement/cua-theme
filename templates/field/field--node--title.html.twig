{#
  field--node--title.html.twig
  Title field renderer — schema handled via JSON-LD in node.html.twig.
#}
{% for item in items %}
  <span{{ item.attributes.addClass('field__item') }}>
    {{ item.content }}
  </span>
{% endfor %}