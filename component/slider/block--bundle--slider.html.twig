{# 
  =============================================================================
  COMPONENT: Accessible Content Slider (Carousel)
  STABILITY: Production-Stable / Architecture: Sinn 556 iRS St (Multi-Movement)
  COMPLIANCE: Section 508 / WCAG 2.1 Level AA / WAI-ARIA Carousel Design Pattern
  =============================================================================
  TECHNICAL ARCHITECTURE:
  - Logic: Prevents "Broken ARIA references" by checking label existence before 
    mapping landmarks.
  - Interactive: Implements aria-roledescription="carousel" to inform assistive 
    tech of the specific widget type.
  - Controls: Hard-coded visually-hidden spans for SR navigation, ensuring 
    navigational clarity even when icons fail to load.

  MAINTENANCE WARNING:
  The carousel is set to data-bs-ride="false". This is a high-end "Coast-Proof" 
  setting—it prevents auto-rotation on page load, which is a major 
  vestibular/ADHD trigger and a common WCAG failure point.
  =============================================================================
#}

{# Asset Load: Selective library injection for the slider movement #}
{{ attach_library('cua_theme/slider') }}

{# Macro Import: Title consistency across the fleet #}
{% import '@cua_theme/component/macros/card_title.html.twig' as titleMacros %}

{# 1. ID ARCHITECTURE: Precise targeting for Bootstrap's JS controller #}
{% set block_content_id = elements.content['#block_content'].id()|replace({'_': '-', ':': '--'}) %}
{% set block_id = 'block--node--slider--' ~ block_content_id %}
{% set carousel_id = 'carousel-' ~ block_content_id %}
{% set title_id = block_id ~ '-title' %}

{# 2. Title Existence Check #}
{% set has_title = label is not empty and label is not null %}

{# 3. Class Orchestration #}
{% set slider_classes = [
    'block',
    'block-' ~ configuration.provider|clean_class,
    'block-' ~ plugin_id|clean_class,
    'slider mx-5 my-1',
    color_background ?: null,
    text_color ?: null,
] %}

{# 4. Landmark Strategy: The "No-Lag" Accessibility Bridge #}
{% set region_aria_attributes = { 'role': 'region' } %}
{% if has_title %}
    {% set region_aria_attributes = region_aria_attributes|merge({ 'aria-labelledby': title_id }) %}
{% else %}
    {% set region_aria_attributes = region_aria_attributes|merge({ 'aria-label': 'Content Slider'|t }) %}
{% endif %}

{# ACCESSIBLE REGION WRAPPER #}
<div{{ attributes
    .addClass(slider_classes)
    .setAttribute('id', block_id)
    .setAttributes(region_aria_attributes) }}>

    {# TITLE LAYER: Only rendered and ID-linked if label exists #}
    {% if has_title %}
        {{ titleMacros.render_title(label, title_prefix, title_suffix, title_id=title_id) }}
    {% endif %}

    {# 
       MAIN CAROUSEL WIDGET: 
       Includes aria-roledescription for legacy screen reader support. 
       We use carousel-dark to ensure controls meet contrast requirements.
    #}
    <div
        id="{{ carousel_id }}"
        class="carousel carousel-dark slide"
        data-bs-ride="false"
        data-bs-interval="10000"
        aria-roledescription="carousel"
        aria-label="{{ 'Image Slider'|t }}"
    >
        {# 
           INNER CONTENT: 
           Expects carousel-inner wrapper and carousel-item divs from content loop. 
        #}
        {{ content }}

        {# NAVIGATION LAYER: Conditional visibility based on CMS field #}
        {% if content.field_visibility_arrows['#items'].0.value == 'visible' %}
            
            {# Previous Control #}
            <button
                class="carousel-control-prev"
                type="button"
                data-bs-target="#{{ carousel_id }}"
                data-bs-slide="prev">
                <span class="fa-solid fa-circle-left" aria-hidden="true"></span>
                <span class="visually-hidden">{{ 'Previous slide'|t }}</span>
            </button>

            {# Next Control #}
            <button
                class="carousel-control-next"
                type="button"
                data-bs-target="#{{ carousel_id }}"
                data-bs-slide="next">
                <span class="fa-solid fa-circle-right" aria-hidden="true"></span>
                <span class="visually-hidden">{{ 'Next slide'|t }}</span>
            </button>
        {% endif %}
    </div>
</div>