{#
/**
 * @file
 * Hidden field template for arrow visibility.
 * This suppresses the raw output of the field value (e.g., 'visible') to prevent it from rendering.
 *
 * Logic for visibility is handled in the parent block template.
 */
#}
{# Intentionally left blank #}